node_modules/
dist/
*.tsbuildinfo

# Secrets — never commit
.env
.env.*
!.env.example

# Local runtime state (tokens live in ~/.3d-print-lister, but guard anyway)
*.sqlite
*.sqlite-journal
tokens.json

# Scratch
tmp/
.cache/

# Local test assets (real sliced 3MFs — user's own prints, not repo content)
testdata/

# Machine-specific Claude Code settings (permissions, local paths)
.claude/settings.local.json
